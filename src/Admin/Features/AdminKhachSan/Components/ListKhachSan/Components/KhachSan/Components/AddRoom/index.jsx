import React from 'react';
import PropTypes from 'prop-types';

AddRoom.propTypes = {
    
};

function AddRoom(props) {
    return (
        <div>
            <h1>Add Room</h1>
        </div>
    );
}

export default AddRoom;